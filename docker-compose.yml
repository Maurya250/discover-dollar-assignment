version: "3.8"

services:

  mongodb:
    image: mongo:latest
    container_name: dd-mongo
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db
    networks:
      - app-network

  backend:
    image: maurya250/discover-dollar-backend:latest
    container_name: backend
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - MONGO_URI=mongodb://mongodb:27017/ddapp
    depends_on:
      - mongodb
    networks:
      - app-network

  nginx:
    image: maurya250/discover-dollar-frontend:latest
    container_name: dd-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - app-network

volumes:
  mongo_data:

networks:
  app-network: